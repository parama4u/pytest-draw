from setuptools import setup

setup(
    name='pytest-sanity',
    version='0.1',
    packages=['pytest-sanity'],
    install_requires=[
        # add any dependencies required by your package
    ],
)